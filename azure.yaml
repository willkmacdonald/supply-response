name: supply-response
metadata:
  template: supply-response@0.1.0
infra:
  provider: bicep
  path: infra
# Services are intentionally omitted. The only supported image path is the
# argument-aware `az acr build` in scripts/deploy_personal_tenant.sh; see runbook.
