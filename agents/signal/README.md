# Signal agent

The frozen prompt extracts explicit supplier facts from bounded typed Evidence Items. It has no tools and no decision authority. The same contract is served by a deterministic local explainer in fallback mode.
