SELECT dataset_id,
       effective_at,
       record_id,
       record_family,
       supplier_id,
       supplier_name,
       part_id,
       part_name,
       plant_id,
       plant_name,
       source_plant_id,
       source_plant_name,
       destination_plant_id,
       destination_plant_name,
       customer_id,
       product_id,
       production_order_id,
       customer_order_id,
       quantity,
       on_hand,
       quality_hold,
       protected_allocation,
       usable_inventory,
       component_demand,
       due_date,
       original_due_date,
       dispatch_date,
       arrival_date,
       incremental_cost_per_unit,
       line_revenue,
       line_margin,
       status,
       audit_complete,
       first_article_complete,
       expected_decision_date,
       data_origin
FROM reporting.operational_records;
